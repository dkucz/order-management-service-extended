# Release v1.6.1

## Date
2026-03-21

## Fixes
- Fixed webhook retry scheduling so completed attempt counts drive both backoff and dead-letter decisions.
- Clarified that cursor pagination remains the public `/api/orders` contract despite swagger drift.

## Operational Notes
- Manual retries from the webhook admin endpoint still preserve historical `attemptCount`.
- Dead jobs continue to age out via the 30-day TTL index.
